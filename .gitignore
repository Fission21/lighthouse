# 状态与运行时产物（不进版本库）
__pycache__/
*.pyc
.DS_Store
config.local.json
windows.local.json
*.log

# 说明：demo/project/docs/.env 是**故意提交**的演示用假密钥文件，
# 用来验证「密钥类文件读不到」。不要把它加进忽略列表。

# 私有注册表/配置的任何备份与变体（*.bak-YYMMDD-HHMMSS 等）也不进库
windows.local.json.*
config.local.json.*
*.bak-*

# 更新日志

## 2026-09-24 · 大库导航 + 核心资料下载提醒

- **资料清单可排序**：点表头「资料 / 状态 / 等级 / 文件夹」就按它排，再点一次反过来；当前列有箭头。
  排序链接带着当前筛选与所在文件夹，不会一跳回根目录。默认顺序不变（文件夹 → 标题）。
- **文件夹能筛**：文件夹区多了个「筛文件夹」框，几十个文件夹时按名字即时筛（纯前端，不刷新、不联网）。
- **最近动态**：资料页底部一块默认收着的「最近动态」（时间 / 谁 / 做了什么 / 哪篇），
  一眼看个大概；要看全量去「用量」页。
- **`kb watch-downloads <窗口>`**：只看「上次看过之后新出现的」高等级资料下载 / 打包 / 限时链接，
  一行中文，没有新的就什么都不输出。默认看住最高一档、不看维护者自己的操作；
  `--levels` / `--skip-principal` / `--include-admin` / `--peek` 可调。给 cron 用（空输出=不发消息）。
- 配套：`core/kb_watch.py`；测试 357 → **376**（新增 ⑮ 大库导航 + 敏感下载守望共 19 项）。

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与语义化版本。

## [未发布]

### 新增
- **管理页拆成四页**：资料（`/admin`）/ 人员（`/admin/people`）/ 邀请码（`/admin/invites`）/ 用量
  （`/admin/usage`）各占一页，页签互跳，POST 完回到自己那一页 —— 原来一屏 4.3 屏高、什么都堆在一起。
- **小字说明改成悬停提示**：解释性文字不再进正文（`title` / `?` 气泡），页面上只留数据、状态回执和空状态一句。
- **一行一次搞定**：资料行的「等级 + 文件夹」合并成一个「保存」（`@apply`），一次提交同时生效。

### 变更
- 每页只保留一个主按钮，其余降级为 `ghost`；一次性/危险动作收进「更多设置 / 更多操作 / ⋯」。
- 视觉刻度对齐《Refactoring UI》：字号 `--f1..--f5` = 12/14/16/18/24，折叠区与空状态不再套虚线框（改用底色），
  `.grid2` 上限 760px，表格数字右对齐（`td.num`）。

- **文件夹管理**：资料库里的文件夹就是磁盘上的真实子目录 —— 新建文件夹、把资料**拖**到别的文件夹、
  批量移动、删除文件夹（整棵进回收站）；台账的 `path` 与 `doc_id` 跟着改，状态/等级/审批记录都保留。
- **文件夹默认等级**：给文件夹设一个默认等级，新进来的文件继承它；优先级
  「上传时显式选的等级 > 所在文件夹（含父文件夹）> 窗口默认」，只影响新文件。
- **回收站**：删单篇或整个文件夹都先进 `<资料目录>/.回收站/`，30 天内可原样放回（状态一并复原），
  也可以彻底删或一键清理 30 天前的。
- **改显示标题**：行内直接改标题，不动磁盘文件名（`doc_id` 按路径算，改磁盘名会打乱判重与已发链接）。
- **同事侧按文件夹浏览**：资料页按文件夹分组（可折叠），每个文件夹可整包下载（只装他等级内的）。
- **AI 侧按文件夹检索**：`kb_list` / `kb_search` 支持 `folder` 限定，返回里带 `folder` 字段。
- **归位两种方式**：人工（拖拽/下拉/批量）+ AI 建议（`kb suggest` 出方案、`kb apply` 才执行，MCP 侧仍零写）。
  `kb suggest` 按**任务需求**聚类（同一个项目/需求下的各种类型文件归到一起），线索是「已有文件夹名命中」
  或「若干篇共享的项目名/任务编号」；**找不到共同任务标识就不建议**（不按文件类型硬塞）。
- 命令行同款：`kb mkdir / mv / title / flevel / trash / suggest / apply`。

### 安全
- `clean_rel()` 对 `..`、`.`、绝对路径、`~`、盘符、隐藏名、超 6 层、怪字符**一律拒绝**，
  不再"悄悄改写"路径（否则「删 /etc/x」会变成删库里的 `etc/x`）。

### 计划中
- 受控资料库：按分类批量授权（现在只能逐篇/逐等级）
- 门户：同事自助改密码
- 大库导航：文件夹过滤框、按上传时间/等级排序、最近操作面板
- 回收站 30 天自动清理（现在要管理员点一下）

## [0.6.0] - 2026-09-24

这一版把「受控资料库」从一个能用的窗口，做成**能给外部同事用的资料门户**，并补齐了开源工程规范。

### 新增
- **受控资料库（kb 模式）**：把资料目录开成只读窗口 —— 只注册 `kb_info / kb_list / kb_search / kb_read / kb_link`
  五个工具，MCP 侧**没有任何写能力**；台账 `catalog.json` 逐篇记录状态，只有「已公开且文件未改」才可读。
- **直接取原件**：门户 `<地址>/files` 可逐篇下载原件或抽取的文本、可打包 zip；`kb_link` 给 15 分钟限时签名链接。
- **一人一条地址**：每个同事一条 `https://<域名>/kb-<口令>`，自带等级与有效期（默认 30 天），可停用、可轮换、可删除。
- **三级资料**：`L1-商务` / `L2-技术` / `L3-核心`；等级不继承，L1 可自助放行，L2/L3 需人工批。
- **账号密码登录**：scrypt 哈希存储、HMAC 签名会话 cookie、改密踢旧会话、连错锁 IP；
  带凭据的地址（管理令/地址令牌/签名链接）不强制登录。
- **邀请码注册**：同事凭管理员发的码自助注册（一码一人、可设有效期与次数、用完即失效），
  全程可追溯「谁、什么时候、用哪张码」。
- **管理页**：一页管资料（上传/扫描/公开/改等级/下架/预览/删除）、待批申请、同事权限、邀请码、用量看板。
  支持拖拽与整文件夹上传、sha256 判重、回执逐文件说明。
- **用量与审计**：每次调用留痕（谁 / 什么工具 / 成功或拒绝 / 资料 / IP / UA），按人、按天、按资料汇总。
- **登录后自动跳转**：登录成功改为 302 直接进目标页（原来停在「正在进入…」的提示页上，得再点一次「继续」）；
  已登录再打开登录页也直接送走。测试助手改为不跟随重定向（否则拿不到 302 上的会话 cookie）
- **登录入口分开**：同事走 `/login`，管理员走 `/admin/login`；走错门会被拦下并指路（都不签发会话）
- **设计令牌与三套主题**：颜色/间距/圆角/字号集中定义，`kb theme <窗口> teal|indigo|paper` 一键换配色（默认 `teal` 松石绿）；
  见 [`docs/DESIGN.md`](docs/DESIGN.md)。
- **工程规范**：`pyproject.toml`（ruff）、`CONTRIBUTING.md`、`.editorconfig`、本文件。

### 变更
- 门户页面统一改用设计令牌，不再写死颜色；一屏一个主按钮；长说明收进 `?` 悬停提示；
  同事列表默认折叠成一行、资料与同事列表分页。

### 修复
- **登录时用户名不存在会直接 500**：那条分支引用了未定义的变量，等于给了「换假用户名绕过 IP 限速」的路子（现在照常统一报错并记一次失败）。
- 页面里给用户复制的链接是相对路径 → 复制出半截链接（现在一律完整地址）。
- 上传回执页会因缺少查询串而 500（`q = q or {}`）。
- 上传时 `after=publish` 会把已存在篇目的等级悄悄改掉（现在只对新增/内容变化的篇目生效）。
- 同事登录态打开管理页只说「需要登录」、没有换账号出路（现在 403 并给「退出这个账号」）。

### 安全
- 审批压不过默认拉黑；等级不做继承；资料内容一改自动退回待批；MCP 侧零写能力。
- 上传路径挡 `../` 与绝对路径、拒收隐藏文件、超限拒收不留残文件。
- 签名下载链接真验签（HMAC + 到期 + 绑定篇目）；门户响应带 `no-store` / `X-Frame-Options` / `Referrer-Policy`。

## [0.5.0] - 2026-09-17

### 新增
- 四道闸（黑名单 / 白名单 / 脱敏 / 审计）、写开关、提权审批（agent 只能申请，用户批）。
- 只读工具面（列目录、读文件、搜索、统计）与写工具面分离。
- `lighthouse.sh` 统一入口：起窗口、publish 隧道、冒烟测试。

## [0.1.0] - 2026-09-10

### 新增
- 最小可用版本：一个本地目录 → 一个 MCP 窗口（stdio/HTTP），带只读审计。
